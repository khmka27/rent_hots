// import Row from 'react-bootstrap/esm/Row';
// import ApartmentCard from './ApartmentCard';
// import React from 'react';

// export default function AllApartments(cards, user) {
//   return (
//     <Row className="mt-3">
//       {cards.map((card) => (
//         <ApartmentCard key={card.id} card={card} user={user} />
//       ))}
//     </Row>
//   );
// }

// // export default function AllObjects({ cards, user }) {
